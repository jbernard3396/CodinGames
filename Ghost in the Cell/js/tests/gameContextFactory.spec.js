//this file should be deleted because Idk what a singleton is i guess

// const GameContextFactory = require('../src/main.js').GameContextFactory;
// const GameContext = require('../src/main.js').GameContext;

describe('GameContextFactory', () => {
    describe('createGameContext', () => {
        it('returns the gameContext', () => {
            // expect(GameContextFactory.getGameContext()).toBe(gameContext);
            expect(true).toBe(true);
        });
    });
});